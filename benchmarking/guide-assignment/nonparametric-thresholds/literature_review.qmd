---
title: "Guide assignment as inference under a latent signal/noise confounder"
subtitle: "One estimand, two strategies, and where our method fits"
author: "SCEPTRE3 guide-assignment investigation"
date: last-modified
format:
  html:
    toc: true
    toc-depth: 3
    number-sections: true
    theme: cosmo
    df-print: kable
    code-fold: true
    embed-resources: true
---

## The one-paragraph version {.unnumbered}

Every guide-assignment method answers the same question for each (guide $g$, cell $c$): **is the
observed count $N_{g,c}$ larger than ambient contamination alone would produce?** Contamination has a
**rank-1** rate $a_g d_c$ (guide ambient abundance × cell ambient exposure), and the obstacle is a
**latent confounder**: each UMI is secretly *signal* or *noise*, and that indicator must be untangled
from a matrix that pools the two. There are exactly **two strategies** for the confounder, and they
sort the methods cleanly. **Model it** — the mixture models (CLEANSER, SCEPTRE, crispat) posit a latent
signal/noise component per guide and integrate it out; this conditions on the confounder and so
sidesteps Simpson's paradox, at the price of per-guide fitting. **Pool then correct it** — the
contingency tests (geomux, fishash, ours) condition on the observed margins and test on a $2\times2$
table; pooling reintroduces the confounder, which surfaces as two failure modes that must be patched.
The $2\times2$ picture is the native object for the *second* strategy only. Read that way, fishash's
noise correction is the contingency family **reaching back toward** the signal/noise conditioning that
mixtures have for free — and our method (depth_fix) completes that reach on the one axis fishash leaves
open, the axis CLEANSER already handles on the mixture side.

---

## The estimand: one test, repeated {#sec-estimand}

Write each observed count as signal plus noise,
$$
N_{g,c} \;=\; S_{g,c} \;+\; Z_{g,c},
$$
where $S_{g,c}$ counts UMIs from a **true integration** of guide $g$ in cell $c$ and $Z_{g,c}$ counts
**ambient contamination** (soup, chimeras, index hopping). Assignment is the family of tests
$$
H_0^{(g,c)}: S_{g,c}=0
\qquad\text{vs}\qquad
H_1^{(g,c)}: S_{g,c}>0 .
$$
Under $H_0$, $N_{g,c}=Z_{g,c}$ is pure noise, so the test reduces to: **is $N_{g,c}$ surprisingly large
for noise?** Everything below is a way to pin down "for noise."

## The latent confounder and the rank-1 noise rate {#sec-confounder}

The reason "for noise" is hard is a hidden variable. Following fishash's formulation, tag every UMI with
a latent indicator $S_u\in\{\text{signal},\text{noise}\}$ — a UMI carrying barcodes $(g,c)$ is *signal*
if it came from a real integration, *noise* otherwise. The barcode distribution differs by stratum, and
the observed distribution pools them:
$$
\pi_{g,c} \;=\; p_{\text{sig}}\,\pi^{\text{signal}}_{g,c} \;+\; p_{\text{noise}}\,\pi^{\text{noise}}_{g,c}.
$$
The two strata have **opposite structure**:

- In the **noise** stratum the guide and cell barcodes are (to good approximation) independent — a
  floating contaminant's identity is unrelated to which droplet it lands in — so
  $$
  \pi^{\text{noise}}_{g,c}=\pi^{\text{noise}}_{g,:}\,\pi^{\text{noise}}_{:,c}
  \quad\Longrightarrow\quad
  \mathbb{E}[Z_{g,c}]=a_g\,d_c \quad(\textbf{rank-1}).
  $$
- The **signal** stratum is the opposite — concentrated on the true $(g,c)$ pairs, maximally
  *non*-independent.

So **detecting signal is detecting a departure from the rank-1 noise rate**, and the whole difficulty is
that $a_g$ and $d_c$ must be estimated from the pooled $\pi_{g,c}$, in which $S_u$ is unobserved. $S_u$
is a genuine latent confounder: ignore it and the (rank-1-violating) signal stratum leaks into your
estimate of the noise rate. Every method is a way to (i) estimate $a_g d_c$ despite $S_u$ and (ii) test
$N_{g,c}$ against it — and they divide by **how they treat $S_u$.**

---

## Two strategies for the confounder {#sec-strategies}

### Strategy 1 — model $S_u$ (mixtures, and thresholds) {#sec-model}

Introduce the signal/noise indicator into the model as a latent **component membership** and integrate
it out. For a guide $g$, fit a two-component distribution to its counts across cells — an *ambient*
component (the noise stratum) and a *native* component (the signal stratum) — and assign by the
posterior $P(\text{native}\mid N_{g,c})$. Because the noise rate $a_g$ is read off the ambient component
— guide $g$'s **own low mode** — it is signal-free by construction. This is the textbook handling of a
latent confounder: **model it, don't pool over it.**

- **crispat (Poisson–Gauss):** Poisson (ambient) + Gaussian (native) on $\log_2$ non-zero counts, per
  guide. No cell factor — $d_c\equiv 1$.
- **SCEPTRE:** per-guide Poisson-GLM mixture $\log\mu_j=\gamma X_j+o_j$, $X_j\sim\mathrm{Bern}(\pi)$; the
  offset $o_j$ carries a per-cell exposure from covariates (incl. library size) — so $d_c$ is modeled,
  but from a **raw, signal-contaminated** covariate.
- **CLEANSER:** NB mixture with guide- **and** cell-specific normalization; the cell term $L_i$ is built
  from the count of $\le 2$-UMI guides — a **signal-free** $d_c$. Alone among the mixtures it
  decontaminates the cell exposure.
- **Otsu / smoothed-valley:** the degenerate case — a hard 1-D split of guide $g$'s distribution at the
  between-mode valley. Modeling $S_u$ with a threshold instead of a likelihood; no $d_c$.

**What this strategy costs.** Fitting is **per guide**, so there is no shared, jointly-estimated rank-1
structure — each guide re-learns its own noise rate, and the cell factor is either ignored (crispat,
Otsu), reintroduced raw (SCEPTRE), or estimated by a separate signal-free device (CLEANSER's $L$). Its
characteristic vulnerability is **mode separability**: when the signal and noise modes overlap (weak
signal, low counts) the latent split is uncertain and the posterior miscalibrates. Crucially, this
strategy does **not** suffer Simpson's paradox — modeling $S_u$ *is* the conditioning that prevents it —
nor self-masking (§ below), because it never forms a shared, signal-contaminated exposure.

### Strategy 2 — pool, then correct (contingency tests) {#sec-pool}

Don't introduce $S_u$; instead condition on the observed **margins** and test $N_{g,c}$ against the
rank-1 independence fit with a one-sided Fisher/hypergeometric test. **Here the $2\times2$ table is the
native object, not an analogy.** Its payoff is that the shared margins estimate a joint rank-1 rate *for
free*: every cell informs $a_g\propto N_{g,:}$, every guide informs $d_c\propto N_{:,c}$, giving $G+C$
parameters for $G\times C$ data-starved entries. Its price is that **pooling signal+noise into the
margins reintroduces exactly the confounder the mixtures modeled away** — and that surfaces as two
failure modes.

- **geomux / our ambient test:** the raw contingency test — rank-1 fit on the full (pooled) matrix, no
  correction. Exposed to both failure modes below.
- **fishash:** the contingency test with the guide-side correction (§Simpson).
- **depth_fix (ours):** fishash plus the cell-side correction (§self-masking).

---

## Inside Strategy 2: the $2\times2$, and its two failure modes {#sec-2x2}

For the pair $(g,c)$ the table (fishash Eq 1–2), written in the $S+Z$ decomposition, is

|                    | cell $c$                          | other cells $-c$                       |
|--------------------|-----------------------------------|----------------------------------------|
| **guide $g$**      | $N_{g,c}=Z_{g,c}$ *(pure noise under $H_0$)* | $N_{g,-c}=Z_{g,-c}+\,S_{g,-c}$ |
| **other guides**   | $N_{-g,c}=Z_{-g,c}+\,S_{-g,c}$    | $N_{-g,-c}=Z_{-g,-c}+\,S_{-g,-c}$      |

with odds ratio $\hat R_{g,c}=\dfrac{N_{g,c}N_{-g,-c}}{N_{-g,c}N_{g,-c}}$, tested one-sided against
$R\le1$. Under $H_0$ the tested cell $N_{g,c}$ is clean noise; **signal leaks into the other three
cells**, and which one you fail to clean determines the failure mode.

### Failure mode A — Simpson's paradox → false positives (*precision*) {#sec-simpson}

The dominant leak is into $N_{-g,-c}$: whatever guides are abundant as signal dump their large counts
into "everything else, elsewhere," inflating the numerator and making a *mere-noise* $N_{g,c}$ look
enriched. fishash's Prop 1 makes it exact — even with $g$ truly absent from $c$ and $g,c$
noise-independent, $\hat R_{g,c}>1$ can hold, a false positive with **no signal in the tested cell at
all**. This is Simpson's paradox: pooling over the latent $S_u$ flips the association. It is *specific to
Strategy 2* — it exists only because the margins pooled signal and noise.

The paper's cartoon (Fig 2a): in cell $c$, $g$ has 1 (noise), others 2 → ratio $0.5$; in other cells,
$g$ has $9$ ($1$ signal $+8$ noise), others $31$ ($23$ signal $+8$ noise) → ratio $0.29$; so
$\hat R\approx1.7>1$, spurious. Recompute the background on **noise only** ($8/8=1$) and $\hat
R^\star=0.5<1$, corrected. fishash estimates those noise-only counts by **rank-1 Poisson completion of
the signal-masked matrix** (Eq 8–9, iterated) — i.e. it re-estimates the noise stratum the pooling
discarded. Anti-conservative, worst in dense/low-SNR regimes → a **precision** problem.

### Failure mode B — self-masking → power loss (*high MOI*) {#sec-self}

The leak into $N_{-g,c}$ is the mirror image. This cell — *other guides in the focal cell* — is
dominated by cell $c$'s **own** true guide(s); it sits in the denominator, so it **deflates**
$\hat R$, making every other guide in a strongly-transduced cell look like noise. Equivalently, the
focal cell's exposure (the hypergeometric "draws" $N_{:,c}$) is overstated by its own signal.
Harmless at low MOI, it bites at **high MOI**, where several real guides in one cell mutually mask —
fishash's documented weak spot. Conservative → a **recall** problem. Again *specific to Strategy 2*: it
is the shared, signal-contaminated exposure that does the masking.

### The tool, and who applies it where {#sec-tool}

Both corrections need the same object — an estimate of the **noise-only** counts $\hat Z=\gamma_g\kappa_c$
— and fishash's masked rank-1 completion supplies it. The mask (assigned entries removed) is itself a
hard estimate of $S_u$; so **fishash is doing, within the contingency frame, a coarse version of what the
mixtures do natively** — estimate the signal/noise split and hold signal out of the noise rate. The
decisive detail: that one fit yields *both* a denoised guide factor $\gamma_g$ **and** a denoised cell
factor $\kappa_c$, but fishash uses $\gamma_g$ (fixing A) and plugs the **raw** $N_{:,c}$ back for the
exposure (leaving B). fishash's corrected p-value (Eq 7) shows it — the draws stay raw:

$$
P_{g,c}=1-F_{\text{hyper}}\!\Big(N_{g,c}-1;\;
\underbrace{\hat N^{\text{noise}}_{:,-c}+N_{:,c}}_{\text{pop}},\;
\underbrace{\hat N^{\text{noise}}_{g,-c}+N_{g,c}}_{\text{success (guide, denoised)}},\;
\underbrace{N_{:,c}}_{\text{draws (cell, RAW)}}\Big).
$$

depth_fix simply uses the $\kappa_c$ fishash already computed. One line in `contingency_method.R`:

```r
K <- nr[i] - bgc + y                              # guide factor: denoised in BOTH modes (fixes A)
if (cell_margin == "ambient") {                   # depth_fix (OURS): also fix B
  draws <- nc[j] - bgc + y                         #   cell exposure = DENOISED ambient depth (kappa_c)
  pop   <- Tn - bgc + y
} else {                                          # cell_margin == "observed"  ==  fishash exactly
  draws <- obs_col[j]                              #   cell exposure = RAW library size N_{:,c}
  pop   <- Tn - nc[j] + obs_col[j]
}
logp <- phyper(y - 1, K, pop - K, draws, lower.tail = FALSE, log.p = TRUE)
```

`cell_margin="observed"` reproduces fishash bit-for-bit; the whole contribution is which quantity
supplies `draws`.

---

## The convergence {#sec-convergence}

The two strategies are two answers to the same latent confounder, and with the corrections in place they
**meet at the same statistical content**: test $N_{g,c}$ against a *signal-free, rank-1* noise rate.

- Mixtures reach it by **modeling** $S_u$ (the component membership) — automatically signal-free on the
  guide factor; CLEANSER additionally makes the cell factor signal-free via $L$.
- Contingency tests reach it by **pooling then re-estimating** the noise stratum — fishash's masked
  completion recovers the signal-free guide factor; depth_fix recovers the signal-free cell factor.

So fishash is the contingency family importing the mixtures' guide-side conditioning, and **depth_fix is
the contingency-family analogue of CLEANSER's cell-side conditioning** — the two families closing the
same two gaps from opposite directions. The trade that remains is intrinsic, not incidental: mixtures buy
robustness to Simpson/self-masking with per-guide fitting (and exposure to mode misspecification);
contingency tests buy shared rank-1 borrowing with a susceptibility to pooling artifacts that must be
patched. depth_fix is the fully-patched contingency corner.

## The map {#sec-map}

Reading each method by the fair, strategy-neutral axes — *how does it estimate the guide noise factor and
the cell exposure, and is each estimate signal-free?* — and its characteristic vulnerability:

| Method | $S_u$ strategy | guide factor $a_g$ | cell exposure $d_c$ | characteristic vulnerability |
|---|---|---|---|---|
| Fixed threshold | none | global constant | none | everything |
| Otsu / valley | model (1-D split) | own low mode (signal-free) | none | no cell adaptation |
| crispat (Poisson–Gauss) | model (mixture) | own low mode (signal-free) | none | no cell adaptation; separability |
| SCEPTRE | model (mixture) | own low mode (signal-free) | raw covariate offset | separability; raw exposure |
| CLEANSER | model (mixture) | own low mode (signal-free) | **signal-free ($L$, $\le2$)** | separability; per-guide |
| geomux | pool (uncorrected) | pooled margin (contaminated) | pooled margin (contaminated) | **Simpson + self-masking** |
| our ambient test | pool (uncorrected) | pooled margin (contaminated) | pooled margin (contaminated) | **Simpson + self-masking** |
| **fishash** | pool + correct | **noise-only (imputed)** | raw library $N_{:,c}$ | **self-masking (high MOI)** |
| **depth_fix (ours)** | pool + correct | **noise-only (imputed)** | **ambient depth (imputed)** | — |

Two things the table makes honest: the mixtures' guide factor is signal-free *by construction* (not by
"cleaning a $2\times2$ cell" — they have no table); and the only decontaminated-cell-exposure methods are
CLEANSER (model side) and depth_fix (pool side), which is the convergence in one column.

## Empirical fingerprints {#sec-empirical}

The framing predicts specific signatures for the *Strategy-2* failure modes; the benchmarks show them.
*(Sim figures are as last recorded; the per-guide-fit sim redesign is edited-but-not-re-run — provisional.)*

- **A is a precision effect.** In hard/low-SNR sim regimes only Simpson-corrected methods reach precision
  $\ge0.90$; the raw contingency tests sit on a lower-precision frontier. The self-contained Fig-7
  reproduction nails the mechanism: uncorrected FDR $\approx0.20$ (fails 5% in 10/10 reps) $\to$ corrected
  $\approx0.003$ (0/10), **recall unchanged**.
- **B is a recall effect, MOI-graded.** Per-guide F1 at MOI 1/3/5: depth_fix **0.96/0.93/0.89** vs fishash
  0.96/0.89/0.82 — identical with no co-occurring guides, diverging as MOI (mutual masking) rises.
- **Real barnyard** (low-MOI, per-cell metric): depth_fix ties fishash (0.937 vs 0.942). The raw ambient
  test scores highest (0.951) by lacking the mode-A brake — the same coin as its low sim precision.
- **The cutoff trap** is the no-strategy corner: fixed $\ge3$ ties depth_fix on easy data but its FDR
  hits $\sim0.31$ on high-ambient data where depth_fix holds $\sim0.027$.
- **CLEANSER**, once a scoring artifact is fixed, is competitive (Jaccard 0.893) — consistent with it
  being the mixture that also decontaminates the cell exposure.

## What is actually ours {#sec-ours}

1. The cell exposure should be a signal-free ambient depth, not raw library size — but **CLEANSER got
   there first** (its $\le2$-count $L$), on the mixture side. We credit it.
2. **fishash decontaminates the guide factor, not the cell exposure** — verified from Eq 7 (draws $=N_{:,c}$)
   and from our reproduction (`cell_margin="observed"`$\equiv$fishash). So on the contingency side the
   ambient exposure is a real addition.
3. The contribution is **completing the pool-and-correct corner**: give the contingency test *both* a
   denoised guide factor (fishash) and a denoised cell exposure (ours), using the $\kappa_c$ fishash's own
   completion already produces and discards. It is the contingency-family analogue of CLEANSER's cell-side
   fix, and it targets fishash's high-MOI weakness — beating it there while tying on real barnyard.

## Open questions {#sec-open}

- **Downstream arbiter.** All metrics here are upstream (assignment vs truth). The deciding test for
  precision-vs-recall trade-offs is downstream **SCEPTRE DE** — negative-control calibration and power
  under each assignment. Not yet run.
- **depth_fix vs a correctly-scored CLEANSER**, head to head — the sharpest test, since CLEANSER is the
  *other* method that decontaminates the cell exposure, from the opposite strategy. Not yet done.
- **Estimator quality of the ambient depth.** CLEANSER's hard $\le2$ cutoff vs our rank-1 noise column —
  which is the better signal-free $d_c$ is untested.
- **Overdispersion.** A conditional NB/beta-binomial tail rescues sim FDR under overdispersion but
  collapses on real barnyard (dispersion not robust across chemistries). Keep the plain hypergeometric;
  revisit only if DE calibration demands it.

## Sources {#sec-refs}

`literature/`: cleanser.pdf (Liu, Hamilton et al. 2025, *Cell Genomics*); fishash.pdf (Kamm et al. 2026,
bioRxiv; package in `external/fishash/`); geomux.pdf (Teyssier 2024, UCSF thesis); crispat.pdf +
crispat-supplement.pdf; sceptre-methods.html. Statistics: Simpson (1951); Fisher (1922); Guo & Sarkar
(2020); Benjamini–Hochberg (1995) / Yekutieli (2001); Fleming et al. (2023, CellBender); Yang et al.
(2020, DecontX); Young & Behjati (2020, SoupX).
