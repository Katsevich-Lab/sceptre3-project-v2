---
title: "sceptre — Bioconductor Submission Plan"
author:
  - name: Eugene Katsevich
date: today
date-format: long
format:
  html:
    toc: true
    toc-depth: 3
    toc-location: left
    toc-title: "Contents"
    number-sections: true
    number-depth: 3
    embed-resources: true
    theme:
      light: cosmo
    fontsize: "1.02em"
    linestretch: 1.55
    smooth-scroll: true
    page-layout: article
    grid:
      sidebar-width: 260px
      body-width: 820px
      margin-width: 220px
    fig-cap-location: bottom
    code-fold: false
    code-tools: false
    link-external-icon: true
    link-external-newwindow: true
    css: custom.css
execute:
  echo: false
  warning: false
---

## Current state

```
R-CMD-check          — passes on macOS, Ubuntu release/oldrel, and Windows
BiocCheck v1.47.26   — 0 ERRORS · 1 WARNING · 5 NOTES
```

Three stacked `sceptre` PRs and one independent book PR are now in flight, and together they should clear the way for Bioconductor submission:

- **[sceptre #229](https://github.com/Katsevich-Lab/sceptre/pull/229) — maintainer metadata.** Makes Eugene Katsevich the package maintainer, adds his ORCID, and records the canonical package URL.
- **[sceptre #228](https://github.com/Katsevich-Lab/sceptre/pull/228) — mixture-assignment disclosure.** Documents the known fitting inconsistency in the current `method = "mixture"` implementation while retaining the established behavior that has performed well in benchmarks. The maximum and thresholding assignment methods are unaffected.
- **[sceptre #230](https://github.com/Katsevich-Lab/sceptre/pull/230) — generated pkgdown site.** Rebuilds the complete site from the combined metadata and disclosure source changes.
- **[sceptre-book #6](https://github.com/timothy-barry/sceptre-book/pull/6) — mixture-method documentation.** Updates the book to distinguish the intended EM derivation from the current implementation and adds focused limitation callouts while preserving the existing derivation and explanatory text.

The three `sceptre` PRs should be reviewed and merged in the order #229 → #228 → #230. The book PR is independent. BiocCheck on the exact combined package source reports that the maintainer is registered at the Support Site and that `sceptre` is among the maintainer's watched tags.

## Bioconductor cover letter

The submitted package has 1 warning and 5 notes, for which we request exemptions:

- **WARNING: No Bioconductor dependencies detected.** The primary container for single-cell data in the Bioconductor ecosystem is SingleCellExperiment. Instead of using SingleCellExperiment to store and manipulate the expression data, we implemented several specialized data structures and algorithms better suited to the specific access patterns required of perturb-seq analysis. After data import (e.g., from a collection of 10x Genomics .mtx files), sceptre computes various cell-wise covariates relevant to perturb-seq analysis, including the gRNA with the greatest UMI count in a given cell and the fraction of UMIs attributable to this gRNA. sceptre does so using optimized C++ routines. Next, sceptre converts the sparse gene and gRNA expression matrices from column-accessible format into row-accessible format. This transformation is crucial, as all subsequent steps -- including QC, gRNA assignment, and gRNA-to-gene association testing -- require rowwise access to the gene and gRNA expression matrices. SingleCellExperiment (and allied packages, such as scran) can store these matrices and covariates, but they do not implement the specific, optimized perturb-seq computations of sceptre.
- **NOTE: Cannot determine whether maintainer is subscribed to the Bioc-Devel mailing list.** The maintainer (Eugene Katsevich, <ekatsevi@wharton.upenn.edu>) is subscribed to the mailing list.
- **NOTE: Update R version dependency from 4.1 to 4.6.0.** We would prefer to avoid excluding users with older versions of R, including those in computer clusters where they do not have control over the version of R installed.
- **NOTE: Avoid 'cat' and 'print' outside of 'show' methods.** This NOTE targets `cat()`/`print()` used in place of `message()`/`warning()` for diagnostic logging. Both flagged instances are instead object-display / file-serialization output — the use the `show()`-method exemption is meant to cover. In `R/s4_helpers.R`, the flagged `cat()` calls are inside the S4 `print()` method for `sceptre_object`, which first delegates to `show(x)` and then renders additional detail (an analysis-status checklist and result summaries) that `show()` omits. In `R/s4_analysis_functs_2.R`, the flagged `print()` call sits inside a `sink()` block that serializes the object's summary to `analysis_summary.txt`.
- **NOTE: Consider shorter lines.** We have reduced lines over 80 characters as much as possible. The remaining long lines (constituting only 2% of our codebase) are impossible to shorten as they contain long URLs, generated `R/RcppExports.R`, or single-line knitr chunk headers.
- **NOTE: The recommended function length is 50 lines or less.** We have deferred addressing this note, as it would require substantial work.
