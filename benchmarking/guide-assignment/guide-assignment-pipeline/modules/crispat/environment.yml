name: crispat
channels:
  - conda-forge
  - pytorch
  - bioconda
dependencies:
  - python=3.12
  - pytorch
  - cpuonly       # This is the magic "make it easy" button for non-GPU users
  - pandas
  - scanpy
  - pip
  - pip:
    - git+https://github.com/jdeu1023/crispat.git@benchmark